# EcoSync-CropGuard

Offline-first crop disease diagnostic Android app for Zimbabwean smallholder farmers.

**Developer:** Joel K. M. — TCFL/SD/2023/007  
**Platform:** Android only (Flutter/Dart)  
**Database:** SQLite (cropguard.db)

---

## Setup

### 1. Generate the database

```bash
python scripts/populate_db.py
```

This creates `assets/db/cropguard.db` with all 5 crops, 25 diseases, 75 symptoms, and 50 treatments.

### 2. Install Flutter dependencies

```bash
flutter pub get
```

### 3. Run the app

```bash
flutter run
```

---

## Project Structure

```
lib/
  main.dart                  — Entry point
  app.dart                   — MaterialApp + Provider setup
  models/                    — Data model classes
  services/                  — DB access layer (DatabaseHelper, DiagnosticService, etc.)
  providers/                 — AppStateProvider (crops, bookmarks)
  screens/                   — All UI screens
  widgets/                   — Reusable UI components
  utils/constants.dart       — Colours, plant parts, shared helpers
scripts/
  populate_db.py             — Generates the SQLite asset database
assets/
  db/cropguard.db            — Pre-populated database (generated by script)
  images/                    — Reference disease images (add manually per disease)
```

## Adding Reference Images

Place images in the correct folder matching the path format in `populate_db.py`:

```
assets/images/maize/gls_1.jpg       ← Grey Leaf Spot image 1
assets/images/maize/nclb_1.jpg      ← NCLB image 1
...
```

Images should be JPEG, max 800×600 px, quality 75 (use ImageMagick to compress):

```bash
magick input.jpg -resize 800x600> -quality 75 output.jpg
```

## Data Sources

All disease/treatment data is sourced from publicly available agronomic literature.
Each treatment record includes a `source` field with the full bibliographic reference.
Key sources: FAO, CIMMYT, CORESTA, CABI CPC, CRI Zimbabwe, Syngenta/Bayer/BASF product labels.
